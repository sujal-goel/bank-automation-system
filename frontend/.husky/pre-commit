#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for staged files
npx lint-staged

# Run type checking (placeholder for JavaScript project)
echo "Type checking: Skipped for JavaScript project"

# Run tests on staged files
npm run test -- --passWithNoTests --findRelatedTests --bail